import { Policy, ChecklistState } from '@/types/policy';

/**
 * Mock data store — intentionally empty so the app starts fresh.
 * Populate via the generator or real API integration.
 */
export const mockPolicies: Policy[] = [];

export const mockChecklistStates: Record<string, ChecklistState> = {};
