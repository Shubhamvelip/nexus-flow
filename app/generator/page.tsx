import { GeneratorPageContent } from '@/components/GeneratorPageContent';

export const metadata = {
  title: 'Policy Generator - Knowledge Workflow',
  description: 'Generate policies from PDF documents using AI',
};

export default function GeneratorPage() {
  return <GeneratorPageContent />;
}
